# PROJECT_RULES.md

חוקי הליבה של פרויקט targilim. חובה לקרוא לפני כל שינוי. ראו גם RULES.md (מלא).

## כללי יסוד
1. חובה לקרוא את RULES.md לפני כל שינוי.
2. **אין דמו / mock / placeholder / "בקרוב" / כפתורים מזויפים / שאלות/גרפים מזויפים.** הכול אמיתי ומחובר ללוגיקה.
3. אין נתוני תלמידים אמיתיים בקוד. אין secrets.
4. אין להשתמש בריפו ישן במקום הריפו הנוכחי.
5. לפני עבודה מקומית יש להריץ `npm run verify:sync` מתוך הריפו הפעיל. אם הבדיקה נכשלת, לא ממשיכים עד שמבינים באיזה עותק עובדים.
6. כל ממשק המשתמש והפלט לתלמיד בעברית RTL. הקרדיט הגלוי: "יניב רז".

## מטרת המוצר — דף עבודה להדפסה (Print-first)
7. **האתר מייצר דפי עבודה להדפסה ותוכן להעתקה ע"י המורה — לא מטלות ממוחשבות לתלמיד.**
8. זרימת העבודה: המורה מפיק שאלות → מעתיק/מדפיס → התלמיד פותר על נייר.
9. אסור להתייחס לשאלות כמטלה אינטראקטיבית מקוונת: אין הגשה מקוונת, אין auto-grading
   כמטרה מרכזית, אין "לחצו/גררו/בחרו במסך" בפלט לתלמיד. פעלי-מסך מותרים רק בבקרות
   המורה (מוסתרות בהדפסה), לא בתוכן המודפס.
10. הפלט מותאם לנייר: ניסוח ברור, פריסת דף-עבודה, מקום לתשובות, מספור, נוטציה
   מתמטית מקצועית, שרטוטים/גרפים ניתנים להדפסה, ללא רכיבים אינטראקטיביים בתוכן התלמיד.
11. רב־ברירה: לתלמיד **אסור** לחשוף את התשובה הנכונה; היא מופיעה רק במפתח התשובות למורה.
    האפשרויות מעורבבות (התשובה הנכונה לא תמיד ראשונה). מסיחים אמיתיים ורלוונטיים.
    המורה בוחר ניסוח: "תשובה אחת נכונה" או "ייתכן יותר מאחת".

## עיגון למקורות (10 קבצים)
12. השאלות מבוססות **אך ורק** על 10 קובצי המקור (`sources/intake/2026-06-09/originals/`)
    ותמלולי הלמידה (`source-learning/2026-06-09/*.learning.md`). אין להמציא נושאים/סוגי
    שאלות שאינם במקורות. אין לזייף חיבור למקור.
13. **קובץ מקור 10** (רצף הוראה) משמש לתכנון בלבד — **לעולם לא כמקור ישיר לשאלה.**

## טווח וארכיטקטורה
14. **טווח: כיתות ז׳–ח׳ בלבד.** כיתה ט׳ מחוץ לתחום (locked) עד שיהיו מקורות ודוגמאות אמיתיים.
15. **מצב המנועים: 50 engine topics / 0 fallback** (ספירה ב-`source-registry.js`, מאומת ב-`verify:source-lock`). **לא 25.**
16. מערכת **דו-שכבתית**: topics מסוג legacy/source-fit (למשל ב-`generator/a8-03.js`) לצד מנועים ייעודיים `*-ENGINE`.
17. **A8-04 = אי-שוויונות** קיים כ-**topic legacy** ב-`a8-03.js`; **אין `A8-04-ENGINE`**. אסור ליצור כפילות A8-04.
18. אסור להוסיף **engine 51** (מנוע חדש) ללא תכנון מלא: עדכון registry + pedagogy + docs + verifiers + ספירת engines.
19. אסור להוסיף engine חדש לפני בדיקה אם topic/engine כבר קיים.

## איכות פלט
20. copy/export שומר שאלה **מלאה**: טקסט + KaTeX + SVG + גרפים + טבלאות.
21. גרפיקה מתמטית מדויקת; צורות מתמטיות אינן כרטיסי UI מעוגלים.

## בטיחות ריפו
22. אסור merge ל-main בלי אישור מפורש · אסור force push · אסור reset --hard / git clean -fd.
23. אסור למחוק: package.json, generator/book.*, tools/verify*, source-learning, sources. אין מחיקה ללא הוכחת אי-תלות + אישור.
24. אסור להכניס _audit/, .claude/, TARGILIM_*_AUDIT*.txt, TARGILIM_*_INTEL*.txt (חסומים ב-.gitignore).
25. אסור להחליש verifier כדי לעבור. מותר לתקן verifier כך שיבדוק את הדרישה הנכונה.
26. `npm run verify:deep` הוא שער האיכות; חייב להישאר ירוק אחרי כל שינוי. PR קטן וממוקד.

## ממשק ושפה (UI)
27. **האתר הוא כלי עבודה, לא דף נחיתה.** העמוד הראשי נפתח ישירות לכרטיס "הגדרות תרגיל" (בחירת נושא). אין מקטע פתיחה/hero/יתרונות/CTA שיווקי.
28. **אין שפה שיווקית/דמו בטקסט הגלוי למשתמש.** ניסוח יבש, ענייני, למורה. אין "דמו", סלוגנים, הבטחות לא-מוכחות, או jargon (fallback/QA/מנוע/Registry).
29. **בורר הרמה הוא `רמה 1` / `רמה 2` / `רמה 3`** (ה-`#sl` הגלוי) ומשפיע באמת על הקושי בכל נושא.
30. **ציורים לא חושפים את התשובה.** הצלע/הזווית/הערך הנעלם מסומן `?`; רק הנתונים מתויגים. תוויות מלאות בתוך ה-viewBox (`text-anchor="middle"`), בלי חיתוך, עם יחידות מלאות (`ס"מ`, לא `ס`).
31. **תוויות נושא וניסוח נאמנים למקור** — מאומת מול `source-learning/` ו-`curriculum-map/`; עברית מתמטית מדויקת (למשל "אורך היתר", לא "היתר"; בפיתגורס משתמשים ב"ניצב/ניצבים", לא "רגל/רגליים").
32. **משבצת פתרון תלמיד יוקרתית ונוחה** — שורות נדיבות; רק לסוגי שאלה פתוחים (open/mistake); ל-mcq/tf אין תיבת כתיבה.
