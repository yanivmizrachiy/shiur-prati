# דוח תיקון: "0/0 מנועים" בדף QA חזותי — 2026-06-14

## הדיווח
במסך QA חזותי הופיע «0 / 0 מנועים», «0 / 0 כרטיסים», והודעה «אין מנועים התואמים
את הסינון», בעוד הדוחות מדווחים 50 מנועים.

## האבחון (ללא ניחוש)
- `git status` נקי, על הראש; `verify:deep` → **23/23 PASS**.
- בדפדפן עם **רענון מלא עוקף-מטמון** של `visual-qa.html` בגרסה הנוכחית
  (commit 222da1f): **50 כרטיסים**, `SOURCE_REGISTRY` טעון (50 מזהי `-ENGINE`),
  הספירה «50 / 50», אזור "ריק" מוסתר, **0 שגיאות קונסול**.

**מסקנה:** הדף תקין ב-commit הנוכחי. ה-0/0 נבע מ**גרסה ישנה במטמון הדפדפן**:
תגיות ה-`<script>` ב-`visual-qa.html` (וב-`gallery.html`) נטענו **ללא פרמטר
גרסה** (בניגוד ל-`index.html` שמשתמש ב-`?v=…`). לכן רענון רגיל הגיש עותק
מטמון ישן (מלפני שהדף/המנועים היו קיימים), `SOURCE_REGISTRY` חזר ריק,
`ids=[]`, והדף הציג 0/0 **בשקט** בלי להסביר למה.

## התיקון (כדי שלא יחזור)
1. **Cache-busting**: לכל תגיות הסקריפט המקומיות ב-`visual-qa.html` ו-`gallery.html`
   נוסף `?v=20260614-vc1` (CDN לא נגעתי). הקישור «QA חזותי» מ-`index.html` קיבל
   `?v=` כך שפתיחה מהמחולל תמיד מביאה דף טרי.
2. **אבחון גלוי במקום 0/0 שקט**: אם `SOURCE_REGISTRY` ריק/חסר בעת האתחול, הדף
   מציג כרטיס אזהרה ברור: «⚠ דף ה-QA לא הצליח לטעון את המנועים … בצעו רענון מלא
   Ctrl+Shift+R / Cmd+Shift+R», מעדכן את הספירה ל«0 / 0 — המנועים לא נטענו»,
   ורושם `console.error`. אותו שומר נוסף ל-`gallery.html`.

## למשתמש
אם עדיין מופיע 0/0 — בצעו **רענון מלא עוקף מטמון**: Windows `Ctrl+Shift+R`,
Mac `Cmd+Shift+R` (או פתיחת DevTools → לחיצה ארוכה על רענון → "Empty Cache and
Hard Reload"). לאחר מכן יופיעו 50 הכרטיסים.

## בדיקות
- `verify:visual-qa` → VISUAL_QA_DASHBOARD_PASS · `verify:gallery` → TEACHER_GALLERY_PASS
  · `verify:links` → PASS · `verify:deep` → 23/23 PASS.
- QA בדפדפן: visual-qa 50/50, gallery 50/50, שני הדפים עם סקריפטים מגורסים,
  ללא כרטיס שגיאה, 0 שגיאות קונסול.
