# דוח QA דפדפן והדפסה — 2026-06-14

נבדק חי בדפדפן דרך שרת סטטי (לא רק בדיקות יחידה ב-Node).

## מחולל (index.html)
- אתחול נקי: ערכת המנועים נטענה, 50 מנועי `-ENGINE`, `window.Teacher` ו-
  `E.generateFollowUpQuestion` קיימים. **0 שגיאות קונסול**.
- מיתוג: הכותרת מציגה «האתר מנוהל ע"י יניב רז».
- הופק דף 5 שאלות מ-G8-05 (זווית מרכזית): 5 כרטיסי שאלה, שרטוטי גזרה רונדרו.
- מצב מורה: הופעל → 5 כרטיסי מורה + 5 סרגלי בקרה; `body.teacher-advanced`.

## בקרות לכל שאלה (אינטראקציה אמיתית)
| פעולה | תוצאה |
|---|---|
| ↻ רענן ×6 | 5 תוצאות שונות (מגוון אמיתי) |
| + קשה יותר | standard → challenge |
| ⇄ סוג שאלה ×5 | עבר על open/mcq/tf/mistake |
| ➟ שאלת המשך (harder) | נוצר בלוק המשך עם תווית מצב ופתרון מתקפל |
| ✓ פתרון | נפתח inline (teacher-only) |
| ⧉ שאלה+פתרון | payload עם מפריד «— פתרון —», ללא תגיות HTML |

## הדפסה
- כל ה-`teacher-only` / `teacher-controls` / `teacher-card` / `follow-up-host`
  מוסתרים ב-`@media print` (אומת ב-`verify:print-layout`).
- שורת שם/תאריך מוצגת; מפתח תשובות מוסתר עד פתיחה ומודפס בעמוד נפרד;
  כרטיסי שאלה לא נחתכים בין עמודים (`break-inside: avoid`).
- כפתורי הפעולה מסומנים `data-html2canvas-ignore` ולכן לא נכנסים לייצוא PNG.

## גלריה (gallery.html)
- 50 כרטיסים, מונה «50 / 50 מנועים»; ללא `<svg>` מוטמע (טעינה מהירה).
- "צור דוגמה" רינדר שאלה+פתרון; סינון תחום/Provenance עובד (מספרי→17, מדויק→17).
- **0 שגיאות קונסול**.

## מסקנה
מצב המורה, הבקרות, ההעתקה/ייצוא, השרטוטים, ההדפסה והגלריה עובדים בדפדפן
ללא שגיאות. תואם ל-`verify:teacher`, `verify:teacher-controls`,
`verify:copy-export`, `verify:print-layout`, `verify:gallery`,
`verify:graphics-quality`.
