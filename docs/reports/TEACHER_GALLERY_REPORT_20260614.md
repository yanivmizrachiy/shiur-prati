# דוח גלריית מנועים — 2026-06-14

## מה נבנה
**`generator/gallery.html`** — עמוד עצמאי המציג את **כל 50 המנועים**. הכרטיסים
נבנים דינמית מ-`E.SOURCE_REGISTRY` (לא רשימה קשיחה), כל כרטיס מציג: מזהה,
נושא, תגיות כיתה/תחום/מקור/provenance, ורשימת המשפחות.

## סינון
כיתה (ז׳/ח׳) · תחום (גאומטריה/אלגברה/מספרי/אי-ודאות) · provenance
(מדויק/ברירת מחדל) · חיפוש חופשי (מזהה/נושא/מיומנות). מונה "X / 50 מנועים".

## ביצועים — אין 50 SVG כבדים במסך הראשי
הגלריה **אינה** מרנדרת דוגמאות מראש: העמוד הסטטי נשלח ללא שום `<svg>`.
דוגמה מופקת רק בלחיצה על "צור דוגמה" (lazy, `E.generateOne`), וכרטיס מורה
בלחיצה על "כרטיס מורה". כך טעינה ראשונה מהירה גם עם 50 מנועים.

## QA בדפדפן
- 50 כרטיסים, מונה "50 / 50 מנועים".
- לחיצה על "צור דוגמה" רינדרה שאלה+פתרון (KaTeX) בכרטיס.
- סינון תחום=מספרי → 17 כרטיסים; provenance=מדויק → 17 (17 המנועים הייעודיים).
- **0 שגיאות קונסול**.

## בדיקות
- `verify:gallery` → TEACHER_GALLERY_PASS (50 מנועים נגישים דרך generateOne,
  טוען את כל הסקריפטים, ללא `<svg>` מוטמע, כל המסננים קיימים).
