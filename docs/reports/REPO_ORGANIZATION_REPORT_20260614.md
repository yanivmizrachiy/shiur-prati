# דוח ארגון ריפו — 2026-06-14

ארגון מקצועי של הריפו ללא שבירת דבר. כל שינוי מבוסס-ראיות (אודיט קודם), כל
ההעברות הן `git mv` (היסטוריה נשמרת), אפס מחיקות, `verify:deep` נשאר 23/23 ירוק.

## מה אורגן (בוצע)
### 1. תיקיית `docs/` — מ-51 ל-21 קבצים בשורש
30 מסמכים היסטוריים ש**אף קוד אינו מסתמך עליהם** ו**ללא קישורים נכנסים** הועברו
לתת-תיקיות לפי תפקיד (אומת לפני ההעברה):
- `docs/source-fit/` (14) · `docs/process/` (6) · `docs/reference/` (5) ·
  `docs/planning/` (3) · `docs/audits/` (2 נוספים).

21 הקבצים שנשארו בשורש הם בדיוק אלה שהקוד/הבדיקות קוראים (SOURCE_BIBLE,
SOURCE_ALIGNMENT, RELEASE_CHECKLIST, WORKLOG, מסמכי SOURCE_FIT_*_EXPANSION
המקושרים מבדיקות) + 8 מסמכים מקושרים-צולבות — **לא הוזזו בכוונה** כדי לא לשבור הפניות.

### 2. אינדקסים חדשים (ניווט = ארגון)
- **`docs/README.md`** — אינדקס מקוטלג של כל התיעוד (כל הקישורים נבדקו, אפס שבורים).
- **`tools/README.md`** — מפת 44 הכלים: 31 verifiers ב-`verify:deep`, 3 מחוללים,
  ספריית `engine-load`, 6 verifiers עצמאיים, ו-harness/release/termux.
- **`README.md`** — נוספה טבלת "מבנה הריפו" + קישור לאינדקס התיעוד.

## ממצאים שאינם תקלות (הושארו בכוונה)
- **`generator/.nojekyll`** — קובץ ריק **בכוונה** (סמן GitHub Pages). נשאר.
- **`tools/` — אין קוד מת**: כל "יתום" לכאורה הוא מחולל (`gen-*`), harness,
  או verifier עצמאי המתועד ב-QA_CHECKLIST/WORKLOG. כלל בטיחות אוסר מחיקת `tools/verify*`.
- מסמכי-ליבה וקבצים מוגנים (sources/, source-learning/, package.json, book.*) — לא נגעתי.

## דורש אישורך לפני מחיקה (DELETE_ONLY_AFTER_APPROVAL) — לא נמחק
1. **10 קבצי PDF כפולים** — כל קובץ מקור קיים פעמיים:
   `sources/intake/2026-06-09/NN-name/NN_*.pdf` **וגם** `sources/intake/2026-06-09/originals/NN_*.pdf`
   (תוכן זהה ב-SHA1). הבדיקות מצביעות על תיקיות ה-`NN-name/`; תיקיית `originals/`
   היא העותק העודף. מחיקת `originals/` תקטין את הריפו, **אך `sources/` מוגן** ולכן
   לא מחקתי — דרוש אישורך.
2. **שני דוחות fallback חופפים** ב-`docs/reports/`:
   `FALLBACK_CONVERSION_REPORT_20260614.md` ו-`FALLBACK_TO_ENGINE_CONVERSION_REPORT_20260614.md`
   (אותו נושא, אף אחד לא מקושר). השארתי את שניהם כתיעוד היסטורי; אפשר לאחד לפי בקשתך.

## אימות
- `verify:deep` → 23/23 PASS. 0 הפניות שבורות לנתיבים הישנים. כל קישורי האינדקסים נפתרים.
- כל ההעברות R100 (rename מלא, היסטוריה נשמרת). 0 מחיקות.
