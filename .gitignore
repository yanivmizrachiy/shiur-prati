# Local audit / intel artifacts — generated by ad-hoc probes, never committed.
_audit/
TARGILIM_*_AUDIT*.txt
TARGILIM_*_INTEL*.txt

# Local editor / preview config
.claude/

# Node
node_modules/

# Learning-material PDFs are bundled into the Pages artifact at deploy time
# (see .github/workflows/deploy-pages.yml). They live in sources/intake/ and are
# copied to generator/assets/sources/ on deploy, so we do not commit the copy.
generator/assets/sources/
