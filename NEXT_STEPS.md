# NEXT_STEPS.md

הצעדים הבאים הנוכחיים. קובץ זה מחליף רשימות ישנות שכבר הושלמו.

## לפני כל עבודה

```powershell
cd C:\Users\yaniv\OneDrive\Dokumente\targilim\.repo-inspect
git fetch origin
npm run verify:sync
npm run verify:workbench
```

אם `verify:sync` נכשל, לא ממשיכים עד שמסדרים את העותק המקומי.

## סדר עדיפות להמשך שיפורים

1. **הרחבת שאלות מתוך המקורות הקיימים בלבד.**
   - לבדוק קודם `docs/SOURCE_BIBLE.md` ו-`generator/engine/pedagogy-registry.js`.
   - לא להוסיף מנוע 51 בלי תכנון מלא ועדכון registry/docs/verifiers.

2. **A7-05 — עוד ייצוגי ביטוי↔טבלה↔גרף.**
   - להוסיף הקשרים בסגנון מקור 01.
   - לשמור על גרפיקה חובה ועל 95%+ כיסוי חזותי.

3. **A8-03 — עוד בעיות מערכת משוואות מהקשר מילולי.**
   - להרחיב את משפחת count/value שכבר מוזגה ב-PR #52.
   - לשמור על פתרונות שלמים וללא מסיחים מלאכותיים.

4. **בדיקת הדפסה ידנית.**
   - A4 עם מפתח תשובות סגור/פתוח.
   - וידוא שבקרות מורה לא מופיעות בהדפסת תלמיד.

5. **בדיקת העתקת תמונה ידנית.**
   - להעתיק שאלה עם גרף/טבלה.
   - להדביק ב-Word/Docs/Canva.
   - לוודא שהטקסט, KaTeX והשרטוט נשמרים.

6. **ניקוי עתידי רק באישור יניב.**
   - לא למחוק PDF כפולים בלי הוכחת אי-תלות.
   - לא למחוק דוחות היסטוריים בלי החלטת superseded וקישור מאינדקס.

## הבהרות חשובות

- multi-correct כבר אינו pending: `A7-04-ENGINE` מאומת ב-`verify:multi-correct`.
- `A8-04` הוא topic legacy/source-fit בתוך `generator/a8-03.js`; אין `A8-04-ENGINE`.
- אין fallback פעיל: 50 engine topics / 0 fallback.
- קובץ מקור 10 הוא לרצף הוראה בלבד, לא מקור ישיר לשאלות.

## חוקי המשך

- `verify:deep` חייב להישאר ירוק.
- אין force push, אין reset hard, אין מחיקה מסוכנת.
- כל שינוי משמעותי: לעדכן סטטוס/יומן, להריץ בדיקות, commit, push.
