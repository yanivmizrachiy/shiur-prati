# NEXT_STEPS.md

הצעדים המדויקים הבאים. עדכן עם ההתקדמות. אל תתחיל פאזה 2 בלי הנחיית יניב.

## פתוח אחרי Phase 1
1. **תוכן רב־ברירה עם יותר מתשובה נכונה אחת (אמיתי):** כרגע מומשו הבורר, הניסוח
   ("ייתכן יותר מאחת") ומפתח תשובות התומך ב-1..N. המנועים עדיין פולטים תשובה נכונה
   אחת. בפאזה 2 ניתן להוסיף מחוללי רב־ברירה רב-נכונה אמיתיים במשפחות שבהן זה טבעי
   ומעוגן-מקור (למשל A7-04 "אילו ביטויים שקולים ל-3(x+2)?" — 2 נכונות; "אילו מספרים
   שליליים?"; "אילו משולשים ישרי-זווית?"). חובה לעדכן את verify-topic-question-variety
   (שמוודא בדיוק תשובה אחת) למצב רב-נכון בלבד, בלי להחליש את הבדיקה למצב single.

2. **אי-שוויונות A8-04 — הבהרת מצב (תיקון בלבול קודם):**
   - **A8-04 = אי-שוויונות ומגבלות**, קיים כ-**topic legacy/source-fit בתוך `generator/a8-03.js`** (לא "אין מנוע כלל").
   - **אין `A8-04-ENGINE` ייעודי** ב-`source-registry.js`. ספירת המנועים היא **50**.
   - **A8-05 = משוואות אחוזים** (`משוואות אחוז`), אף הוא topic legacy ב-`a8-03.js`.
   - **PR #11** (`a804final`) מחזק את A8-04 ה-legacy (משנה רק `a8-03.js`) — מועמד לשמירה אחרי בדיקה.
   - **PR #10** (`phase2a805`) מוסיף `A8-04-ENGINE` ל-registry (engine 51) — **לא למזג** ללא תכנון מלא.
   - הפיכת A8-04 ל-dedicated ENGINE דורשת תכנון מלא (registry+docs+verifiers+ספירה), **לא PR קטן**.
3. **כיסוי-עומק נוסף (פאזה 2, מעוגן-מקור):** השפעת שינוי על ממוצע/חציון/טווח (U-09),
   יחס שטחים = ריבוע יחס הדמיון (G8-06), זיהוי פונקציה מעבר למיפוי בודד.

4. **דורש אישור יניב לפני מחיקה (לא בוצע) — PR נפרד בלבד:**
   - 10 קובצי PDF כפולים: `sources/intake/2026-06-09/originals/` זהים ל-`NN-name/` (מועמד למחיקה אחרי הוכחת אי-תלות).
   - שני דוחות fallback חופפים ב-docs/reports/ (להשאיר כהיסטוריה או לאחד ב-PR נפרד).

## חוקי המשך
- `verify:deep` חייב להישאר ירוק. אין merge ל-main / force push. שינויים מינימליים, אמיתיים, מעוגני-מקור.
- כל שינוי → עדכן REQUIREMENTS_STATUS.md + WORK_LOG.md, הרץ verify:deep, commit, push.
